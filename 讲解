# 两个页面讲解脚本（HyperOS 首页 + HyperConnect 跨端智联页）

> 用法：按屏幕从上往下滚的顺序走。每段三块——**【看到什么】**给听众指、**【怎么实现】**讲技术、**【口播】**是可以直接念的一句话。不用全背，记住每段的口播即可。

---

## 0. 开场白（约 30 秒）

> 「我做的是两个落地页，模仿小米 HyperOS 的官网风格。整体是**原生 HTML/CSS/JS** 手写，只引入了四个轻量库来负责动画和滚动。设计上有个原则：**该用库的地方用库，简单交互用原生**，并且做了完整的降级和性能优化。下面我先讲技术栈，再带大家逐屏看效果。」

---

## 1. 技术栈总览（先建立框架，再讲细节）

一句话概括分工：**GSAP 管动画，Lenis 管平滑滚动，vanilla-tilt 管卡片倾斜，Bootstrap 管栅格，剩下的小交互用原生 JS。**

| 库 | 角色 | 用在哪 |
|---|---|---|
| **GSAP 3 + ScrollTrigger** | 动画核心引擎（CDN 引入） | 两个页面所有进场/滚动动画 |
| **Lenis** | 平滑滚动（惯性、果冻感） | 两个页面 |
| **vanilla-tilt** | 鼠标 3D 倾斜 + 高光 | 跨端智联页的卡片 |
| **Bootstrap 5** | 响应式栅格 / 按钮 | 仅跨端智联页 |

> 口播：「动画全部交给 **GSAP**——它是前端最主流的动画库；滚动平滑交给 **Lenis**；卡片的 3D 倾斜用 **vanilla-tilt**；布局用 **Bootstrap 栅格**。值得一提的是首页**没用 Bootstrap**，布局是我用纯 CSS Grid 手写的，按需取用、不臃肿。」

**加载顺序也有讲究**（如果被问到可以答）：库要先于自己的脚本加载，而 **Lenis 必须排在动画脚本之前**，因为动画脚本启动时要拿到 Lenis 实例来联动。

---

## 2. 首页（HyperOS）逐屏讲解

### ① 导航栏
- **【看到什么】**顶部导航；往下滚会自动隐藏，往上滚立刻出现；背景在深色/浅色区块间自动换黑白字。
- **【怎么实现】**纯原生 JS。隐藏/显示用滚动方向判断 + 一个「抖动死区阀门」防误触；换肤用 `IntersectionObserver` 监测当前顶部接触的是深色还是浅色版块。
- **【口播】**「导航栏会**感知背景明暗自动换肤**，还会**滚动时让路、回看时归位**，这两个都是原生 JS 做的，没用库。」

### ② Hero 首屏 — “Xiaomi HyperOS”
- **【看到什么】**进页面时数字、Logo、副标题**依次冒出**；往下滚文字**上飘并淡出**；背景固定不动。
- **【怎么实现】**GSAP `timeline` 做依次入场（用负时间做错峰）；滚动淡出用 ScrollTrigger 的 `scrub`，把动画进度**绑死在滚动条上**；背景固定是 CSS `background-attachment: fixed`。
- **【口播】**「入场是 GSAP 时间轴**错峰冒出**；往下滚的淡出用了 ScrollTrigger 的 `scrub`，意思是**动画进度和滚动条绑死**，往下滚动画走、往上滚动画倒退。」

### ③ 性能区 — “更流畅的基础体验”
- **【看到什么】**几张毛玻璃卡片**依次涟漪入场**；里面的**数字从 0 飙升**到目标值。
- **【怎么实现】**卡片错峰用 GSAP 的 `stagger`；数字滚动是个小技巧——让 GSAP 去动画一个虚拟计数对象，每帧把它的值写回文字。毛玻璃是 CSS `backdrop-filter`。
- **【口播】**「卡片用 GSAP 的 `stagger` 做**依次入场**；数字这个 count-up 效果，是让 GSAP **从 0 渐变到目标数**，每一帧把数字写回页面。」

### ④ 系统动画区 / ⑤ 人车家生态区
- **【看到什么】**整块滚到视口就**淡入上滑**；生态那张大图随滚动有**视差错位感**。
- **【怎么实现】**淡入是通用规则：元素顶部到视口 85% 就触发。视差是把图片在容器内从 `+12%` 推到 `-12%`，配合外层裁切，制造图片和页面不同步的立体感。
- **【口播】**「这块是**滚动到就淡入**；大图的**视差**是让图片移动速度和滚动条不同步，做出纵深感。」

### ⑥ 超级岛 / ⑦ 信息卡片 / ⑧ 拖拽区
- **【看到什么】**两列卡片布局，进场依次冒出，悬停有微交互。
- **【怎么实现】**布局是**纯 CSS Grid 两列**（强调：首页这里没用 Bootstrap），进场动画还是 GSAP。
- **【口播】**「这几块的两列布局是我用 **CSS Grid 手写**的，没依赖框架。」

### ⑨ 艺术锁屏 — “全新艺术锁屏，千人千面”
- **【看到什么】**一排图片**横向无缝循环滚动**，鼠标悬停会暂停。
- **【怎么实现】**纯原生 JS 跑马灯：把内容**复制一份接在尾部**，用 `setInterval` 每 20ms 让 `scrollLeft++`，滚到第一份末尾就**瞬间归零**，肉眼看是无缝的；鼠标进出用清除/重建定时器实现暂停。
- **【口播】**「这个横向跑马灯是原生实现的：**内容首尾相接 + 滚动位置归零**做出无缝错觉，鼠标悬停暂停。」

### ⑩ 页脚
- 声明列表 + 底部信息栏，纯 CSS 布局；GSAP 淡入（页脚太矮，触发点单独做了修正）。

---

## 3. 跨端智联页（HyperConnect）逐屏讲解

### ① Hero 首屏 — “HyperConnect / 融合设备中心”
- **【看到什么】**进场文字依次浮现；**鼠标在首屏移动时，标题、副标题按不同深度轻微跟随**（分层立体感）；背景固定。
- **【怎么实现】**进场用 GSAP `stagger`；鼠标分层视差是原生 JS——每个文字层给一个不同的「深度系数」，按鼠标位置算位移。仅桌面端启用，且等入场动画播完才绑定。
- **【口播】**「首屏的亮点是**鼠标分层视差**：标题动得多、副标题动得少，营造纵深，这是原生 JS 算的。」

### ② 三张手机截图（Bootstrap 栅格）
- **【看到什么】**手机端整行堆叠、电脑端自动分三列；进场依次冒出。
- **【怎么实现】**Bootstrap 的 `row / col-md-4` 响应式栅格；进场是 GSAP 的 `reveal-group`（容器加个 class，子元素自动排队入场）。
- **【口播】**「布局用 **Bootstrap 栅格**，手机一列、电脑三列自动切换；进场是我封装的 `reveal-group`，加个 class 子元素就自动错峰出场。」

### ③ ★全站最难一段★ 跨设备能力 — 滚动钉住切换（5 屏）
- **【看到什么】**滚到这块，**画面被钉住不动**，随着滚动**一屏一屏切换**五个能力：贴贴分享 → 应用接力 → 跨设备剪贴板 → 小米互传 → 小米妙播；右侧小圆点同步高亮；还有**吸附**手感（轻轻一滚就跳到下一条）。
- **【怎么实现】**ScrollTrigger 三件套：`pin` 把这一屏**钉死在视口**、`pinSpacing` 在底下垫出滚动空间、`snap` 做吸附；`onUpdate` 里拿到 0~1 的滚动进度，乘面板数取整，**算出当前该亮哪一屏**，切换 CSS 类。**只在桌面端启用**，窗口缩放会动态在手机/电脑版之间切换。
- **【口播】**「这是整个项目最核心的一段。用 ScrollTrigger 的 `pin` 把画面**钉在屏幕上不动**，再根据滚动进度切换内部的五屏内容，配合 `snap` 做出**一滑一切**的段落感。手机端会自动退化成普通竖排。」

### ④ 卡片区（互联兼容 / 苹果·Windows 互联）
- **【看到什么】**卡片悬停会**随鼠标 3D 倾斜并有高光扫过**。
- **【怎么实现】**vanilla-tilt：HTML 上写个 `data-tilt` 属性，JS 一行 `init` 就生效。
- **【口播】**「卡片的 **3D 倾斜**是 vanilla-tilt 插件做的，标签上加个 `data-tilt` 属性就能跟着鼠标转、还带高光。」

### ⑤ 全局小组件（两页通用）
- **顶部滚动进度条**：原生 JS，按滚动百分比改宽度。
- **返回顶部按钮**：滚过一屏浮现，点了调用 **Lenis 平滑滚回顶部**。
- 两者都用 `requestAnimationFrame` 节流，且**由 JS 动态创建插入**，不写死在 HTML 里。
- **【口播】**「顶部进度条和返回顶部按钮都是原生 JS **动态生成**的，滚动监听用 rAF 节流，性能友好。」

---

## 4. 贯穿全站的工程亮点（这部分最能加分，务必讲）

> 评委/老师往往更看重「工程素养」而不是炫效果。这四点挑两三个讲。

1. **渐进增强 / 降级**：动画脚本开头先检查 GSAP 是否加载成功、用户**是否开了系统「减弱动态效果」无障碍设置**——任一不满足就直接退出，页面退化成静态版仍可正常阅读。
   > 口播：「如果用户开了**防眩晕**的无障碍设置，我会自动关掉所有动画——这是无障碍标准的要求。」

2. **异步资源加载的坐标校准（最实用的坑）**：图片/视频异步加载完会把页面撑高，导致滚动触发点全部错位。我监听了所有图片视频的加载事件，用**防抖**重新校准 ScrollTrigger 坐标，加载完还做了两次延时兜底重算。
   > 口播：「图片加载完会把页面撑高、让滚动动画错位，所以我**每次资源加载完都重新校准**滚动坐标，并做了防抖避免卡顿。」

3. **三库共用一套渲染节拍**：让 GSAP 的高效心跳去驱动 Lenis，三者共享同一帧循环，高刷屏（120/144Hz）上才不会打架。

4. **数据驱动 + 作用域隔离**：每个脚本都用 IIFE 包起来防止变量污染全局；那个「技术讲解模式」把「哪个区域显示什么技术」写成一张数据表，加标注只改数据不改逻辑。

---

## 5. 收尾（约 15 秒）

> 「总结一下：两个页面的核心是 **GSAP + ScrollTrigger 做动画、Lenis 做平滑滚动**，配合少量原生 JS 处理交互细节，并在**降级、性能、无障碍**上做了完整处理。我还内置了一个**技术讲解模式**，可以现场演示——谢谢大家。」

---

## 6. 现场杀手锏：打开「技术讲解模式」

讲完后，点页面**左下角的「🎓 技术讲解」按钮**，再点页面任意区域——右侧会弹出该区域用到的技术清单并高亮框出来。这是最直观的演示，也能证明你对每块都门儿清。（按 `Esc` 关闭。）

---

## 7. 可能被问到的问题（提前准备）

**Q：为什么首页不用 Bootstrap，跨端页却用？**
A：首页布局简单，CSS Grid 手写更轻、更可控；跨端页有多处响应式多列，用 Bootstrap 栅格更省事。按需取用。

**Q：`scrub` 和普通进场动画的区别？**
A：普通进场是「触发后自己播完」；`scrub` 是「进度绑在滚动条上」，往上滚会倒放，用于视差这类要严格跟手的效果。

**Q：那个钉住切换是怎么知道该显示第几屏的？**
A：ScrollTrigger 给一个 0 到 1 的滚动进度，乘以面板数再四舍五入，就得到当前索引，据此切换 active 类。

**Q：手机上这些效果还有吗？**
A：平滑滚动、进场动画都在；但「钉住切换」「鼠标视差」「卡片倾斜」这类依赖鼠标或大屏的效果会自动关闭，退化成普通竖向布局。

**Q：性能怎么保证？**
A：滚动监听用 `requestAnimationFrame` 节流、资源校准用防抖、尊重无障碍设置、GSAP 本身是高性能的 transform 动画——综合下来很流畅。

**Q：动画库为什么选 GSAP？**
A：它是业界最成熟的动画库，时间轴、错峰、滚动绑定都很顺手，且 ScrollTrigger 这种滚动场景几乎是行业标准。

---

### 名词小抄（怕一时想不起来时瞟一眼）
- **GSAP**：动画引擎；**ScrollTrigger**：把动画绑到滚动的插件
- **scrub**：动画进度跟滚动条；**pin**：把元素钉在屏幕上；**snap**：滚动吸附；**stagger**：错峰
- **Lenis**：平滑滚动；**vanilla-tilt**：3D 倾斜；**IntersectionObserver**：检测元素是否进入视口（用于导航换肤）
- **背景固定**：`background-attachment: fixed`；**毛玻璃**：`backdrop-filter`
